program produtopar;

var
    N, i, p, r: int64;
    
begin

    read(N);
    i:= 0;
    p:= 2;
    r:= 1;
    
        while (i < N) do 
        begin
        
            r:= r * p;
            p:= p + 2;
            i:= i + 1;
            
        end;
        
    write(r);
    
end.
    
